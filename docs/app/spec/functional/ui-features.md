# UI機能仕様書

**作成日**: 2026-02-28
**ステータス**: ドラフト v0.1
**対象フェーズ**: フェーズ1（静的SPA）

> 本ドキュメントはユーザー向けUI機能（F-5〜F-13）の機能仕様を定義する。
> 実装設計は以下を参照:
> - コンポーネント設計: [`docs/app/spec/system/component-design.md`](../system/component-design.md)
> - 状態管理設計: [`docs/app/spec/system/state-management.md`](../system/state-management.md)
> - 外部API連携設計: [`docs/app/spec/system/api-integration.md`](../system/api-integration.md)

---

## 機能一覧

| ID | 機能領域 | 機能名 | 優先度 |
|----|---------|-------|--------|
| F-5 | 書籍一覧 | キーワード検索 | 必須 |
| F-6 | 書籍一覧 | ジャンルフィルタ（2階層） | 必須 |
| F-7 | 書籍一覧 | 著者絞り込み | 必須 |
| F-8 | 書籍一覧 | ソート | 必須 |
| F-9 | 書籍一覧 | ページネーション | 必須 |
| F-10 | 書籍詳細 | 基本情報表示 | 必須 |
| F-11 | 書籍詳細 | 外部書誌API補完表示 | 推奨 |
| F-12 | 統計・分析 | ジャンル別統計 | 必須 |
| F-13 | 統計・分析 | 著者別ランキング | 必須 |

---

## 書籍一覧・検索機能（S-1）

### F-5: キーワード検索

#### 概要

入力したキーワードで書籍一覧をリアルタイム絞り込みする。

#### 仕様

| 項目 | 仕様 |
|------|------|
| 検索対象フィールド | `title`（タイトル）、`author`（著者名） |
| マッチ方式 | 部分一致（大文字小文字を区別しない） |
| 複数キーワード | スペース区切りで入力した場合は AND 検索 |
| 表示タイミング | 入力と同時にリアルタイム絞り込み（インクリメンタルサーチ） |
| 入力クリア | 検索フィールドのリセットボタンで全件表示に戻る |

---

### F-6: ジャンルフィルタ（2階層）

#### 概要

大ジャンル・サブジャンルの2階層でジャンルフィルタを行い、書籍一覧を絞り込む。

#### 仕様

| 項目 | 仕様 |
|------|------|
| 第1階層 | 大ジャンル（11種）＋「すべて」をボタンで表示 |
| 第2階層 | 大ジャンル選択後、そのジャンルに存在するサブジャンル＋「すべて」を表示 |
| 選択方式 | 各階層とも単一選択 |
| 初期状態 | 大ジャンル「すべて」選択（全件表示）、サブジャンル非表示 |
| サブジャンルなし | `subgenre` が `null` の書籍はサブジャンル「その他」として扱う |
| キーワード検索との組み合わせ | AND 条件で絞り込む |

#### 操作フロー

```
大ジャンル: [すべて] [フィクション] [ノンフィクション] ...
               ↓ 「フィクション」を選択
大ジャンル: [すべて] [フィクション★] ...
小ジャンル: [すべて] [ミステリー] [歴史・時代] [その他フィクション]
               ↓ 「ミステリー」を選択 → フィクション×ミステリーで絞り込む
```

---

### F-7: 著者絞り込み

#### 概要

著者名をクリックまたは選択して、その著者の書籍のみ表示する。

#### 仕様

| 項目 | 仕様 |
|------|------|
| 操作方法 | 書籍一覧・書籍詳細の著者名クリック、または著者名ドロップダウン |
| 絞り込み結果 | 選択した著者の全書籍を表示 |
| 解除方法 | 「すべて」または著者名を再クリックで解除 |
| キーワード検索・ジャンルフィルタとの組み合わせ | AND 条件で絞り込む |

---

### F-8: ソート

#### 概要

書籍一覧の表示順序を切り替える。

#### 仕様

| 項目 | 仕様 |
|------|------|
| ソートキー | 書名（五十音順）、著者名（五十音順）、ページ数（昇順・降順） |
| 初期状態 | 書名の五十音順 |
| UI | ドロップダウンまたはカラムヘッダークリック |

---

### F-9: ページネーション

#### 概要

書籍一覧を複数ページに分けて表示する。

#### 仕様

| 項目 | 仕様 |
|------|------|
| 1ページあたりの表示件数 | 50件 |
| ページ切り替え | 前/次ページボタン、ページ番号直接指定 |
| 件数表示 | 絞り込み結果の総件数と現在表示範囲を表示（例: `1-50 / 819件`） |
| フィルタ変更時の挙動 | フィルタやソート変更時は1ページ目に戻る |

---

## 書籍詳細機能（S-2）

### F-10: 基本情報表示

#### 概要

書籍一覧で選択した書籍の詳細情報を表示する。基本情報はファイル名パースから取得した情報を表示する。

#### 表示項目

| 項目 | データソース | 非存在時の表示 |
|------|------------|--------------|
| タイトル | `books.json` | — |
| 著者名 | `books.json` | — |
| ジャンル（大ジャンル / サブジャンル） | `books.json` | サブジャンルが `null` の場合は大ジャンルのみ表示 |
| シリーズ名 | `books.json` | 非表示 |
| ISBN | `books.json` | 「情報なし」 |
| ページ数 | `books.json` | 「情報なし」 |
| 保有バージョン | `books.json` | — |
| Google Drive リンク | `books.json`（`version_files`） | — |

#### Google Drive リンク仕様

| 項目 | 仕様 |
|------|------|
| 表示形式 | 保有バージョンごとにリンクボタンを表示（例: 「オリジナル版で開く」「Kindle版で開く」） |
| 動作 | クリックで Google Drive の PDF を新しいタブで開く |
| 利用環境 | PC・iPad のブラウザから Google Drive へのアクセス権限が必要 |

---

### F-11: 外部書誌API補完表示

#### 概要

ISBNが存在する書籍について、外部書誌APIから追加情報を取得して詳細画面に表示する。

#### API呼び出し仕様

| 優先度 | API | 用途 | 呼び出し条件 |
|--------|-----|------|------------|
| 第1優先 | OpenBD API | 日本語書籍の書誌情報取得 | ISBNが存在する場合 |
| フォールバック | Google Books API | OpenBDでデータなしの場合（洋書対応） | OpenBDでデータが取得できなかった場合 |

> ISBNが存在しない書籍は外部API呼び出しを行わず、F-10の基本情報のみ表示する。

#### 補完表示項目

| 項目 | 取得元 | 表示条件 |
|------|--------|---------|
| 表紙画像 | OpenBD / Google Books | 取得できた場合 |
| 出版社 | OpenBD / Google Books | 取得できた場合 |
| 出版年 | OpenBD / Google Books | 取得できた場合 |
| 内容紹介・説明文 | OpenBD / Google Books | 取得できた場合 |

#### エラー・ローディング処理

| 状態 | 表示内容 |
|------|---------|
| API呼び出し中 | ローディングインジケータを表示 |
| データなし（APIから空レスポンス） | 「外部情報なし」と表示 |
| APIエラー | エラーメッセージを表示し、基本情報のみ表示継続 |

---

## 統計・分析機能（S-3）

### F-12: ジャンル別統計

#### 概要

蔵書全体のジャンル分布を視覚的に表示する。

#### 表示仕様

| 項目 | 仕様 |
|------|------|
| グラフ形式 | 棒グラフまたは円グラフ（比率表示） |
| 表示内容（第1階層） | 全11大ジャンルの冊数と全体に占める割合（%） |
| 表示内容（第2階層） | 大ジャンルを選択するとサブジャンル別の内訳を表示 |
| インタラクション | グラフのジャンル/サブジャンルをクリックすると書籍一覧をその条件で絞り込む |

---

### F-13: 著者別ランキング

#### 概要

保有冊数の多い著者のランキングを表示する。

#### 表示仕様

| 項目 | 仕様 |
|------|------|
| 表示件数 | 上位20名 |
| 表示内容 | 順位、著者名、保有冊数、代表ジャンル |
| インタラクション | 著者名をクリックすると書籍一覧をその著者で絞り込む |

---

## 画面構成

### 画面一覧

| 画面ID | 画面名 | 主な機能 |
|--------|--------|---------|
| S-1 | 書籍一覧画面 | F-5〜F-9（検索・フィルタ・ソート・ページネーション） |
| S-2 | 書籍詳細画面 | F-10〜F-11（基本情報・API補完情報） |
| S-3 | 統計ダッシュボード画面 | F-12〜F-13（ジャンル分布・著者ランキング） |

### 画面遷移

```
書籍一覧（S-1）
  ├── 書籍カードをクリック → 書籍詳細（S-2）
  │   └── 著者名をクリック → 書籍一覧（S-1）著者絞り込み状態
  └── タブ切替 → 統計ダッシュボード（S-3）
      ├── ジャンルグラフをクリック → 書籍一覧（S-1）ジャンルフィルタ状態
      └── 著者名をクリック → 書籍一覧（S-1）著者絞り込み状態
```

---

## スコープ外（フェーズ1）

| 機能 | 理由 |
|------|------|
| AI 読書レコメンド | バックエンドサーバーが必要なためフェーズ2へ |
| 読書記録（既読/未読・評価・感想） | 将来検討 |
| 蔵書データの定期自動更新 | 将来検討 |
| 著者名の表記揺れ正規化 | 将来検討 |
